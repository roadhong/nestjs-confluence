/**
 * The Confluence Cloud REST API v2
 * This document describes Confluence\'s v2 APIs. This is intended to be an iteration on the existing Confluence Cloud REST API with improvements in both endpoint definitions and performance.
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
/* tslint:disable:no-unused-variable member-ordering */

import { Injectable, Optional } from '@nestjs/common';
import { HttpService } from '@nestjs/axios';
import type { AxiosRequestConfig, AxiosResponse } from 'axios';
import { Observable, from, of, switchMap } from 'rxjs';
import { ContentProperty } from '../model/contentProperty';
import { ContentPropertyCreateRequest } from '../model/contentPropertyCreateRequest';
import { ContentPropertySortOrder } from '../model/contentPropertySortOrder';
import { ContentPropertyUpdateRequest } from '../model/contentPropertyUpdateRequest';
import { MultiEntityResultContentProperty } from '../model/multiEntityResultContentProperty';
import { Configuration } from '../configuration';
import { COLLECTION_FORMATS } from '../variables';


@Injectable()
export class ContentPropertiesService {

    protected basePath = 'https://no-default/wiki/api/v2';
    public defaultHeaders: Record<string,string> = {};
    public configuration = new Configuration();
    protected httpClient: HttpService;

    constructor(httpClient: HttpService, @Optional() configuration: Configuration) {
        this.configuration = configuration || this.configuration;
        this.basePath = configuration?.basePath || this.basePath;
        this.httpClient = configuration?.httpClient || httpClient;
    }

    /**
     * @param consumes string[] mime-types
     * @return true: consumes contains 'multipart/form-data', false: otherwise
     */
    private canConsumeForm(consumes: string[]): boolean {
        const form = 'multipart/form-data';
        return consumes.includes(form);
    }

    /**
     * Create content property for attachment
     * Creates a new content property for an attachment.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to update the attachment.
     * @param attachment_id The ID of the attachment to create a property for.
     * @param ContentPropertyCreateRequest The content property to be created
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [createAttachmentPropertyOpts.config] Override http request option.
     */
    public createAttachmentProperty(attachment_id: string, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createAttachmentPropertyOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public createAttachmentProperty(attachment_id: string, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createAttachmentPropertyOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (attachment_id === null || attachment_id === undefined) {
            throw new Error('Required parameter attachment_id was null or undefined when calling createAttachmentProperty.');
        }

        if (ContentPropertyCreateRequest === null || ContentPropertyCreateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyCreateRequest was null or undefined when calling createAttachmentProperty.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<ContentProperty>(`${this.basePath}/attachments/${encodeURIComponent(String(attachment_id))}/properties`,
                    ContentPropertyCreateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...createAttachmentPropertyOpts?.config,
                        headers: {...headers, ...createAttachmentPropertyOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Create content property for blog post
     * Creates a new property for a blogpost.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to update the blog post.
     * @param blogpost_id The ID of the blog post to create a property for.
     * @param ContentPropertyCreateRequest The content property to be created
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [createBlogpostPropertyOpts.config] Override http request option.
     */
    public createBlogpostProperty(blogpost_id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createBlogpostPropertyOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public createBlogpostProperty(blogpost_id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createBlogpostPropertyOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (blogpost_id === null || blogpost_id === undefined) {
            throw new Error('Required parameter blogpost_id was null or undefined when calling createBlogpostProperty.');
        }

        if (ContentPropertyCreateRequest === null || ContentPropertyCreateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyCreateRequest was null or undefined when calling createBlogpostProperty.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<ContentProperty>(`${this.basePath}/blogposts/${encodeURIComponent(String(blogpost_id))}/properties`,
                    ContentPropertyCreateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...createBlogpostPropertyOpts?.config,
                        headers: {...headers, ...createBlogpostPropertyOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Create content property for comment
     * Creates a new content property for a comment.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to update the comment.
     * @param comment_id The ID of the comment to create a property for.
     * @param ContentPropertyCreateRequest The content property to be created
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [createCommentPropertyOpts.config] Override http request option.
     */
    public createCommentProperty(comment_id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createCommentPropertyOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public createCommentProperty(comment_id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createCommentPropertyOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (comment_id === null || comment_id === undefined) {
            throw new Error('Required parameter comment_id was null or undefined when calling createCommentProperty.');
        }

        if (ContentPropertyCreateRequest === null || ContentPropertyCreateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyCreateRequest was null or undefined when calling createCommentProperty.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<ContentProperty>(`${this.basePath}/comments/${encodeURIComponent(String(comment_id))}/properties`,
                    ContentPropertyCreateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...createCommentPropertyOpts?.config,
                        headers: {...headers, ...createCommentPropertyOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Create content property for custom content
     * Creates a new content property for a piece of custom content.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to update the custom content.
     * @param custom_content_id The ID of the custom content to create a property for.
     * @param ContentPropertyCreateRequest The content property to be created
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [createCustomContentPropertyOpts.config] Override http request option.
     */
    public createCustomContentProperty(custom_content_id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createCustomContentPropertyOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public createCustomContentProperty(custom_content_id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createCustomContentPropertyOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (custom_content_id === null || custom_content_id === undefined) {
            throw new Error('Required parameter custom_content_id was null or undefined when calling createCustomContentProperty.');
        }

        if (ContentPropertyCreateRequest === null || ContentPropertyCreateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyCreateRequest was null or undefined when calling createCustomContentProperty.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<ContentProperty>(`${this.basePath}/custom-content/${encodeURIComponent(String(custom_content_id))}/properties`,
                    ContentPropertyCreateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...createCustomContentPropertyOpts?.config,
                        headers: {...headers, ...createCustomContentPropertyOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Create content property for database
     * Creates a new content property for a database.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to update the database.
     * @param id The ID of the database to create a property for.
     * @param ContentPropertyCreateRequest The content property to be created
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [createDatabasePropertyOpts.config] Override http request option.
     */
    public createDatabaseProperty(id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createDatabasePropertyOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public createDatabaseProperty(id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createDatabasePropertyOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling createDatabaseProperty.');
        }

        if (ContentPropertyCreateRequest === null || ContentPropertyCreateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyCreateRequest was null or undefined when calling createDatabaseProperty.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<ContentProperty>(`${this.basePath}/databases/${encodeURIComponent(String(id))}/properties`,
                    ContentPropertyCreateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...createDatabasePropertyOpts?.config,
                        headers: {...headers, ...createDatabasePropertyOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Create content property for folder
     * Creates a new content property for a folder.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to update the folder.
     * @param id The ID of the folder to create a property for.
     * @param ContentPropertyCreateRequest The content property to be created
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [createFolderPropertyOpts.config] Override http request option.
     */
    public createFolderProperty(id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createFolderPropertyOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public createFolderProperty(id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createFolderPropertyOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling createFolderProperty.');
        }

        if (ContentPropertyCreateRequest === null || ContentPropertyCreateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyCreateRequest was null or undefined when calling createFolderProperty.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<ContentProperty>(`${this.basePath}/folders/${encodeURIComponent(String(id))}/properties`,
                    ContentPropertyCreateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...createFolderPropertyOpts?.config,
                        headers: {...headers, ...createFolderPropertyOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Create content property for page
     * Creates a new content property for a page.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to update the page.
     * @param page_id The ID of the page to create a property for.
     * @param ContentPropertyCreateRequest The content property to be created
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [createPagePropertyOpts.config] Override http request option.
     */
    public createPageProperty(page_id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createPagePropertyOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public createPageProperty(page_id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createPagePropertyOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (page_id === null || page_id === undefined) {
            throw new Error('Required parameter page_id was null or undefined when calling createPageProperty.');
        }

        if (ContentPropertyCreateRequest === null || ContentPropertyCreateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyCreateRequest was null or undefined when calling createPageProperty.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<ContentProperty>(`${this.basePath}/pages/${encodeURIComponent(String(page_id))}/properties`,
                    ContentPropertyCreateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...createPagePropertyOpts?.config,
                        headers: {...headers, ...createPagePropertyOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Create content property for Smart Link in the content tree
     * Creates a new content property for a Smart Link in the content tree.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to update the Smart Link in the content tree.
     * @param id The ID of the Smart Link in the content tree to create a property for.
     * @param ContentPropertyCreateRequest The content property to be created
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [createSmartLinkPropertyOpts.config] Override http request option.
     */
    public createSmartLinkProperty(id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createSmartLinkPropertyOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public createSmartLinkProperty(id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createSmartLinkPropertyOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling createSmartLinkProperty.');
        }

        if (ContentPropertyCreateRequest === null || ContentPropertyCreateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyCreateRequest was null or undefined when calling createSmartLinkProperty.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<ContentProperty>(`${this.basePath}/embeds/${encodeURIComponent(String(id))}/properties`,
                    ContentPropertyCreateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...createSmartLinkPropertyOpts?.config,
                        headers: {...headers, ...createSmartLinkPropertyOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Create content property for whiteboard
     * Creates a new content property for a whiteboard.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to update the whiteboard.
     * @param id The ID of the whiteboard to create a property for.
     * @param ContentPropertyCreateRequest The content property to be created
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [createWhiteboardPropertyOpts.config] Override http request option.
     */
    public createWhiteboardProperty(id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createWhiteboardPropertyOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public createWhiteboardProperty(id: number, ContentPropertyCreateRequest: ContentPropertyCreateRequest, createWhiteboardPropertyOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling createWhiteboardProperty.');
        }

        if (ContentPropertyCreateRequest === null || ContentPropertyCreateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyCreateRequest was null or undefined when calling createWhiteboardProperty.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.post<ContentProperty>(`${this.basePath}/whiteboards/${encodeURIComponent(String(id))}/properties`,
                    ContentPropertyCreateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...createWhiteboardPropertyOpts?.config,
                        headers: {...headers, ...createWhiteboardPropertyOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Delete content property for attachment by id
     * Deletes a content property for an attachment by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to attachment the page.
     * @param attachment_id The ID of the attachment the property belongs to.
     * @param property_id The ID of the property to be deleted.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [deleteAttachmentPropertyByIdOpts.config] Override http request option.
     */
    public deleteAttachmentPropertyById(attachment_id: string, property_id: number, deleteAttachmentPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public deleteAttachmentPropertyById(attachment_id: string, property_id: number, deleteAttachmentPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (attachment_id === null || attachment_id === undefined) {
            throw new Error('Required parameter attachment_id was null or undefined when calling deleteAttachmentPropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling deleteAttachmentPropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.delete<any>(`${this.basePath}/attachments/${encodeURIComponent(String(attachment_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...deleteAttachmentPropertyByIdOpts?.config,
                        headers: {...headers, ...deleteAttachmentPropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Delete content property for blogpost by id
     * Deletes a content property for a blogpost by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the blog post.
     * @param blogpost_id The ID of the blog post the property belongs to.
     * @param property_id The ID of the property to be deleted.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [deleteBlogpostPropertyByIdOpts.config] Override http request option.
     */
    public deleteBlogpostPropertyById(blogpost_id: number, property_id: number, deleteBlogpostPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public deleteBlogpostPropertyById(blogpost_id: number, property_id: number, deleteBlogpostPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (blogpost_id === null || blogpost_id === undefined) {
            throw new Error('Required parameter blogpost_id was null or undefined when calling deleteBlogpostPropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling deleteBlogpostPropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.delete<any>(`${this.basePath}/blogposts/${encodeURIComponent(String(blogpost_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...deleteBlogpostPropertyByIdOpts?.config,
                        headers: {...headers, ...deleteBlogpostPropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Delete content property for comment by id
     * Deletes a content property for a comment by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the comment.
     * @param comment_id The ID of the comment the property belongs to.
     * @param property_id The ID of the property to be deleted.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [deleteCommentPropertyByIdOpts.config] Override http request option.
     */
    public deleteCommentPropertyById(comment_id: number, property_id: number, deleteCommentPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public deleteCommentPropertyById(comment_id: number, property_id: number, deleteCommentPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (comment_id === null || comment_id === undefined) {
            throw new Error('Required parameter comment_id was null or undefined when calling deleteCommentPropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling deleteCommentPropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.delete<any>(`${this.basePath}/comments/${encodeURIComponent(String(comment_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...deleteCommentPropertyByIdOpts?.config,
                        headers: {...headers, ...deleteCommentPropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Delete content property for custom content by id
     * Deletes a content property for a piece of custom content by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the custom content.
     * @param custom_content_id The ID of the custom content the property belongs to.
     * @param property_id The ID of the property to be deleted.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [deleteCustomContentPropertyByIdOpts.config] Override http request option.
     */
    public deleteCustomContentPropertyById(custom_content_id: number, property_id: number, deleteCustomContentPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public deleteCustomContentPropertyById(custom_content_id: number, property_id: number, deleteCustomContentPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (custom_content_id === null || custom_content_id === undefined) {
            throw new Error('Required parameter custom_content_id was null or undefined when calling deleteCustomContentPropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling deleteCustomContentPropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.delete<any>(`${this.basePath}/custom-content/${encodeURIComponent(String(custom_content_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...deleteCustomContentPropertyByIdOpts?.config,
                        headers: {...headers, ...deleteCustomContentPropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Delete content property for database by id
     * Deletes a content property for a database by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the database.
     * @param database_id The ID of the database the property belongs to.
     * @param property_id The ID of the property to be deleted.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [deleteDatabasePropertyByIdOpts.config] Override http request option.
     */
    public deleteDatabasePropertyById(database_id: number, property_id: number, deleteDatabasePropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public deleteDatabasePropertyById(database_id: number, property_id: number, deleteDatabasePropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (database_id === null || database_id === undefined) {
            throw new Error('Required parameter database_id was null or undefined when calling deleteDatabasePropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling deleteDatabasePropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.delete<any>(`${this.basePath}/databases/${encodeURIComponent(String(database_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...deleteDatabasePropertyByIdOpts?.config,
                        headers: {...headers, ...deleteDatabasePropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Delete content property for folder by id
     * Deletes a content property for a folder by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the folder.
     * @param folder_id The ID of the folder the property belongs to.
     * @param property_id The ID of the property to be deleted.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [deleteFolderPropertyByIdOpts.config] Override http request option.
     */
    public deleteFolderPropertyById(folder_id: number, property_id: number, deleteFolderPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public deleteFolderPropertyById(folder_id: number, property_id: number, deleteFolderPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (folder_id === null || folder_id === undefined) {
            throw new Error('Required parameter folder_id was null or undefined when calling deleteFolderPropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling deleteFolderPropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.delete<any>(`${this.basePath}/folders/${encodeURIComponent(String(folder_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...deleteFolderPropertyByIdOpts?.config,
                        headers: {...headers, ...deleteFolderPropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Delete content property for page by id
     * Deletes a content property for a page by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the page.
     * @param page_id The ID of the page the property belongs to.
     * @param property_id The ID of the property to be deleted.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [deletePagePropertyByIdOpts.config] Override http request option.
     */
    public deletePagePropertyById(page_id: number, property_id: number, deletePagePropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public deletePagePropertyById(page_id: number, property_id: number, deletePagePropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (page_id === null || page_id === undefined) {
            throw new Error('Required parameter page_id was null or undefined when calling deletePagePropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling deletePagePropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.delete<any>(`${this.basePath}/pages/${encodeURIComponent(String(page_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...deletePagePropertyByIdOpts?.config,
                        headers: {...headers, ...deletePagePropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Delete content property for Smart Link in the content tree by id
     * Deletes a content property for a Smart Link in the content tree by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the Smart Link in the content tree.
     * @param embed_id The ID of the Smart Link in the content tree the property belongs to.
     * @param property_id The ID of the property to be deleted.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [deleteSmartLinkPropertyByIdOpts.config] Override http request option.
     */
    public deleteSmartLinkPropertyById(embed_id: number, property_id: number, deleteSmartLinkPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public deleteSmartLinkPropertyById(embed_id: number, property_id: number, deleteSmartLinkPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (embed_id === null || embed_id === undefined) {
            throw new Error('Required parameter embed_id was null or undefined when calling deleteSmartLinkPropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling deleteSmartLinkPropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.delete<any>(`${this.basePath}/embeds/${encodeURIComponent(String(embed_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...deleteSmartLinkPropertyByIdOpts?.config,
                        headers: {...headers, ...deleteSmartLinkPropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Delete content property for whiteboard by id
     * Deletes a content property for a whiteboard by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the whiteboard.
     * @param whiteboard_id The ID of the whiteboard the property belongs to.
     * @param property_id The ID of the property to be deleted.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [deleteWhiteboardPropertyByIdOpts.config] Override http request option.
     */
    public deleteWhiteboardPropertyById(whiteboard_id: number, property_id: number, deleteWhiteboardPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<any>>;
    public deleteWhiteboardPropertyById(whiteboard_id: number, property_id: number, deleteWhiteboardPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (whiteboard_id === null || whiteboard_id === undefined) {
            throw new Error('Required parameter whiteboard_id was null or undefined when calling deleteWhiteboardPropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling deleteWhiteboardPropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.delete<any>(`${this.basePath}/whiteboards/${encodeURIComponent(String(whiteboard_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...deleteWhiteboardPropertyByIdOpts?.config,
                        headers: {...headers, ...deleteWhiteboardPropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content properties for attachment
     * Retrieves all Content Properties tied to a specified attachment.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the attachment.
     * @param attachment_id The ID of the attachment for which content properties should be returned.
     * @param key Filters the response to return a specific content property with matching key (case sensitive).
     * @param sort Used to sort the result by a particular field.
     * @param cursor Used for pagination, this opaque cursor will be returned in the &#x60;next&#x60; URL in the &#x60;Link&#x60; response header. Use the relative URL in the &#x60;Link&#x60; header to retrieve the &#x60;next&#x60; set of results.
     * @param limit Maximum number of attachments per result to return. If more results exist, use the &#x60;Link&#x60; header to retrieve a relative URL that will return the next set of results.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getAttachmentContentPropertiesOpts.config] Override http request option.
     */
    public getAttachmentContentProperties(attachment_id: string, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getAttachmentContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<MultiEntityResultContentProperty>>;
    public getAttachmentContentProperties(attachment_id: string, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getAttachmentContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (attachment_id === null || attachment_id === undefined) {
            throw new Error('Required parameter attachment_id was null or undefined when calling getAttachmentContentProperties.');
        }

        let queryParameters = new URLSearchParams();
        if (key !== undefined && key !== null) {
            queryParameters.append('key', <any>key);
        }
        if (sort !== undefined && sort !== null) {
            queryParameters.append('sort', <any>sort);
        }
        if (cursor !== undefined && cursor !== null) {
            queryParameters.append('cursor', <any>cursor);
        }
        if (limit !== undefined && limit !== null) {
            queryParameters.append('limit', <any>limit);
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<MultiEntityResultContentProperty>(`${this.basePath}/attachments/${encodeURIComponent(String(attachment_id))}/properties`,
                    {
                        params: queryParameters,
                        withCredentials: this.configuration.withCredentials,
                        ...getAttachmentContentPropertiesOpts?.config,
                        headers: {...headers, ...getAttachmentContentPropertiesOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content property for attachment by id
     * Retrieves a specific Content Property by ID that is attached to a specified attachment.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the attachment.
     * @param attachment_id The ID of the attachment for which content properties should be returned.
     * @param property_id The ID of the content property to be returned
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getAttachmentContentPropertiesByIdOpts.config] Override http request option.
     */
    public getAttachmentContentPropertiesById(attachment_id: string, property_id: number, getAttachmentContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public getAttachmentContentPropertiesById(attachment_id: string, property_id: number, getAttachmentContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (attachment_id === null || attachment_id === undefined) {
            throw new Error('Required parameter attachment_id was null or undefined when calling getAttachmentContentPropertiesById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling getAttachmentContentPropertiesById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<ContentProperty>(`${this.basePath}/attachments/${encodeURIComponent(String(attachment_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getAttachmentContentPropertiesByIdOpts?.config,
                        headers: {...headers, ...getAttachmentContentPropertiesByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content properties for blog post
     * Retrieves all Content Properties tied to a specified blog post.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the blog post.
     * @param blogpost_id The ID of the blog post for which content properties should be returned.
     * @param key Filters the response to return a specific content property with matching key (case sensitive).
     * @param sort Used to sort the result by a particular field.
     * @param cursor Used for pagination, this opaque cursor will be returned in the &#x60;next&#x60; URL in the &#x60;Link&#x60; response header. Use the relative URL in the &#x60;Link&#x60; header to retrieve the &#x60;next&#x60; set of results.
     * @param limit Maximum number of attachments per result to return. If more results exist, use the &#x60;Link&#x60; header to retrieve a relative URL that will return the next set of results.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getBlogpostContentPropertiesOpts.config] Override http request option.
     */
    public getBlogpostContentProperties(blogpost_id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getBlogpostContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<MultiEntityResultContentProperty>>;
    public getBlogpostContentProperties(blogpost_id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getBlogpostContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (blogpost_id === null || blogpost_id === undefined) {
            throw new Error('Required parameter blogpost_id was null or undefined when calling getBlogpostContentProperties.');
        }

        let queryParameters = new URLSearchParams();
        if (key !== undefined && key !== null) {
            queryParameters.append('key', <any>key);
        }
        if (sort !== undefined && sort !== null) {
            queryParameters.append('sort', <any>sort);
        }
        if (cursor !== undefined && cursor !== null) {
            queryParameters.append('cursor', <any>cursor);
        }
        if (limit !== undefined && limit !== null) {
            queryParameters.append('limit', <any>limit);
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<MultiEntityResultContentProperty>(`${this.basePath}/blogposts/${encodeURIComponent(String(blogpost_id))}/properties`,
                    {
                        params: queryParameters,
                        withCredentials: this.configuration.withCredentials,
                        ...getBlogpostContentPropertiesOpts?.config,
                        headers: {...headers, ...getBlogpostContentPropertiesOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content property for blog post by id
     * Retrieves a specific Content Property by ID that is attached to a specified blog post.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the blog post.
     * @param blogpost_id The ID of the blog post for which content properties should be returned.
     * @param property_id The ID of the property being requested
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getBlogpostContentPropertiesByIdOpts.config] Override http request option.
     */
    public getBlogpostContentPropertiesById(blogpost_id: number, property_id: number, getBlogpostContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public getBlogpostContentPropertiesById(blogpost_id: number, property_id: number, getBlogpostContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (blogpost_id === null || blogpost_id === undefined) {
            throw new Error('Required parameter blogpost_id was null or undefined when calling getBlogpostContentPropertiesById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling getBlogpostContentPropertiesById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<ContentProperty>(`${this.basePath}/blogposts/${encodeURIComponent(String(blogpost_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getBlogpostContentPropertiesByIdOpts?.config,
                        headers: {...headers, ...getBlogpostContentPropertiesByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content properties for comment
     * Retrieves Content Properties attached to a specified comment.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the comment.
     * @param comment_id The ID of the comment for which content properties should be returned.
     * @param key Filters the response to return a specific content property with matching key (case sensitive).
     * @param sort Used to sort the result by a particular field.
     * @param cursor Used for pagination, this opaque cursor will be returned in the &#x60;next&#x60; URL in the &#x60;Link&#x60; response header. Use the relative URL in the &#x60;Link&#x60; header to retrieve the &#x60;next&#x60; set of results.
     * @param limit Maximum number of attachments per result to return. If more results exist, use the &#x60;Link&#x60; header to retrieve a relative URL that will return the next set of results.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getCommentContentPropertiesOpts.config] Override http request option.
     */
    public getCommentContentProperties(comment_id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getCommentContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<MultiEntityResultContentProperty>>;
    public getCommentContentProperties(comment_id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getCommentContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (comment_id === null || comment_id === undefined) {
            throw new Error('Required parameter comment_id was null or undefined when calling getCommentContentProperties.');
        }

        let queryParameters = new URLSearchParams();
        if (key !== undefined && key !== null) {
            queryParameters.append('key', <any>key);
        }
        if (sort !== undefined && sort !== null) {
            queryParameters.append('sort', <any>sort);
        }
        if (cursor !== undefined && cursor !== null) {
            queryParameters.append('cursor', <any>cursor);
        }
        if (limit !== undefined && limit !== null) {
            queryParameters.append('limit', <any>limit);
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<MultiEntityResultContentProperty>(`${this.basePath}/comments/${encodeURIComponent(String(comment_id))}/properties`,
                    {
                        params: queryParameters,
                        withCredentials: this.configuration.withCredentials,
                        ...getCommentContentPropertiesOpts?.config,
                        headers: {...headers, ...getCommentContentPropertiesOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content property for comment by id
     * Retrieves a specific Content Property by ID that is attached to a specified comment.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the comment.
     * @param comment_id The ID of the comment for which content properties should be returned.
     * @param property_id The ID of the content property being requested.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getCommentContentPropertiesByIdOpts.config] Override http request option.
     */
    public getCommentContentPropertiesById(comment_id: number, property_id: number, getCommentContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public getCommentContentPropertiesById(comment_id: number, property_id: number, getCommentContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (comment_id === null || comment_id === undefined) {
            throw new Error('Required parameter comment_id was null or undefined when calling getCommentContentPropertiesById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling getCommentContentPropertiesById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<ContentProperty>(`${this.basePath}/comments/${encodeURIComponent(String(comment_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getCommentContentPropertiesByIdOpts?.config,
                        headers: {...headers, ...getCommentContentPropertiesByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content properties for custom content
     * Retrieves Content Properties tied to a specified custom content.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the custom content.
     * @param custom_content_id The ID of the custom content for which content properties should be returned.
     * @param key Filters the response to return a specific content property with matching key (case sensitive).
     * @param sort Used to sort the result by a particular field.
     * @param cursor Used for pagination, this opaque cursor will be returned in the &#x60;next&#x60; URL in the &#x60;Link&#x60; response header. Use the relative URL in the &#x60;Link&#x60; header to retrieve the &#x60;next&#x60; set of results.
     * @param limit Maximum number of attachments per result to return. If more results exist, use the &#x60;Link&#x60; header to retrieve a relative URL that will return the next set of results.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getCustomContentContentPropertiesOpts.config] Override http request option.
     */
    public getCustomContentContentProperties(custom_content_id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getCustomContentContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<MultiEntityResultContentProperty>>;
    public getCustomContentContentProperties(custom_content_id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getCustomContentContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (custom_content_id === null || custom_content_id === undefined) {
            throw new Error('Required parameter custom_content_id was null or undefined when calling getCustomContentContentProperties.');
        }

        let queryParameters = new URLSearchParams();
        if (key !== undefined && key !== null) {
            queryParameters.append('key', <any>key);
        }
        if (sort !== undefined && sort !== null) {
            queryParameters.append('sort', <any>sort);
        }
        if (cursor !== undefined && cursor !== null) {
            queryParameters.append('cursor', <any>cursor);
        }
        if (limit !== undefined && limit !== null) {
            queryParameters.append('limit', <any>limit);
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<MultiEntityResultContentProperty>(`${this.basePath}/custom-content/${encodeURIComponent(String(custom_content_id))}/properties`,
                    {
                        params: queryParameters,
                        withCredentials: this.configuration.withCredentials,
                        ...getCustomContentContentPropertiesOpts?.config,
                        headers: {...headers, ...getCustomContentContentPropertiesOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content property for custom content by id
     * Retrieves a specific Content Property by ID that is attached to a specified custom content.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the page.
     * @param custom_content_id The ID of the custom content for which content properties should be returned.
     * @param property_id The ID of the content property being requested.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getCustomContentContentPropertiesByIdOpts.config] Override http request option.
     */
    public getCustomContentContentPropertiesById(custom_content_id: number, property_id: number, getCustomContentContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public getCustomContentContentPropertiesById(custom_content_id: number, property_id: number, getCustomContentContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (custom_content_id === null || custom_content_id === undefined) {
            throw new Error('Required parameter custom_content_id was null or undefined when calling getCustomContentContentPropertiesById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling getCustomContentContentPropertiesById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<ContentProperty>(`${this.basePath}/custom-content/${encodeURIComponent(String(custom_content_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getCustomContentContentPropertiesByIdOpts?.config,
                        headers: {...headers, ...getCustomContentContentPropertiesByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content properties for database
     * Retrieves Content Properties tied to a specified database.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the database.
     * @param id The ID of the database for which content properties should be returned.
     * @param key Filters the response to return a specific content property with matching key (case sensitive).
     * @param sort Used to sort the result by a particular field.
     * @param cursor Used for pagination, this opaque cursor will be returned in the &#x60;next&#x60; URL in the &#x60;Link&#x60; response header. Use the relative URL in the &#x60;Link&#x60; header to retrieve the &#x60;next&#x60; set of results.
     * @param limit Maximum number of attachments per result to return. If more results exist, use the &#x60;Link&#x60; header to retrieve a relative URL that will return the next set of results.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getDatabaseContentPropertiesOpts.config] Override http request option.
     */
    public getDatabaseContentProperties(id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getDatabaseContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<MultiEntityResultContentProperty>>;
    public getDatabaseContentProperties(id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getDatabaseContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getDatabaseContentProperties.');
        }

        let queryParameters = new URLSearchParams();
        if (key !== undefined && key !== null) {
            queryParameters.append('key', <any>key);
        }
        if (sort !== undefined && sort !== null) {
            queryParameters.append('sort', <any>sort);
        }
        if (cursor !== undefined && cursor !== null) {
            queryParameters.append('cursor', <any>cursor);
        }
        if (limit !== undefined && limit !== null) {
            queryParameters.append('limit', <any>limit);
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<MultiEntityResultContentProperty>(`${this.basePath}/databases/${encodeURIComponent(String(id))}/properties`,
                    {
                        params: queryParameters,
                        withCredentials: this.configuration.withCredentials,
                        ...getDatabaseContentPropertiesOpts?.config,
                        headers: {...headers, ...getDatabaseContentPropertiesOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content property for database by id
     * Retrieves a specific Content Property by ID that is attached to a specified database.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the database.
     * @param database_id The ID of the database for which content properties should be returned.
     * @param property_id The ID of the content property being requested.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getDatabaseContentPropertiesByIdOpts.config] Override http request option.
     */
    public getDatabaseContentPropertiesById(database_id: number, property_id: number, getDatabaseContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public getDatabaseContentPropertiesById(database_id: number, property_id: number, getDatabaseContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (database_id === null || database_id === undefined) {
            throw new Error('Required parameter database_id was null or undefined when calling getDatabaseContentPropertiesById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling getDatabaseContentPropertiesById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<ContentProperty>(`${this.basePath}/databases/${encodeURIComponent(String(database_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getDatabaseContentPropertiesByIdOpts?.config,
                        headers: {...headers, ...getDatabaseContentPropertiesByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content properties for folder
     * Retrieves Content Properties tied to a specified folder.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the folder.
     * @param id The ID of the folder for which content properties should be returned.
     * @param key Filters the response to return a specific content property with matching key (case sensitive).
     * @param sort Used to sort the result by a particular field.
     * @param cursor Used for pagination, this opaque cursor will be returned in the &#x60;next&#x60; URL in the &#x60;Link&#x60; response header. Use the relative URL in the &#x60;Link&#x60; header to retrieve the &#x60;next&#x60; set of results.
     * @param limit Maximum number of attachments per result to return. If more results exist, use the &#x60;Link&#x60; header to retrieve a relative URL that will return the next set of results.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getFolderContentPropertiesOpts.config] Override http request option.
     */
    public getFolderContentProperties(id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getFolderContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<MultiEntityResultContentProperty>>;
    public getFolderContentProperties(id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getFolderContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getFolderContentProperties.');
        }

        let queryParameters = new URLSearchParams();
        if (key !== undefined && key !== null) {
            queryParameters.append('key', <any>key);
        }
        if (sort !== undefined && sort !== null) {
            queryParameters.append('sort', <any>sort);
        }
        if (cursor !== undefined && cursor !== null) {
            queryParameters.append('cursor', <any>cursor);
        }
        if (limit !== undefined && limit !== null) {
            queryParameters.append('limit', <any>limit);
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<MultiEntityResultContentProperty>(`${this.basePath}/folders/${encodeURIComponent(String(id))}/properties`,
                    {
                        params: queryParameters,
                        withCredentials: this.configuration.withCredentials,
                        ...getFolderContentPropertiesOpts?.config,
                        headers: {...headers, ...getFolderContentPropertiesOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content property for folder by id
     * Retrieves a specific Content Property by ID that is attached to a specified folder.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the folder.
     * @param folder_id The ID of the folder for which content properties should be returned.
     * @param property_id The ID of the content property being requested.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getFolderContentPropertiesByIdOpts.config] Override http request option.
     */
    public getFolderContentPropertiesById(folder_id: number, property_id: number, getFolderContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public getFolderContentPropertiesById(folder_id: number, property_id: number, getFolderContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (folder_id === null || folder_id === undefined) {
            throw new Error('Required parameter folder_id was null or undefined when calling getFolderContentPropertiesById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling getFolderContentPropertiesById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<ContentProperty>(`${this.basePath}/folders/${encodeURIComponent(String(folder_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getFolderContentPropertiesByIdOpts?.config,
                        headers: {...headers, ...getFolderContentPropertiesByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content properties for page
     * Retrieves Content Properties tied to a specified page.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the page.
     * @param page_id The ID of the page for which content properties should be returned.
     * @param key Filters the response to return a specific content property with matching key (case sensitive).
     * @param sort Used to sort the result by a particular field.
     * @param cursor Used for pagination, this opaque cursor will be returned in the &#x60;next&#x60; URL in the &#x60;Link&#x60; response header. Use the relative URL in the &#x60;Link&#x60; header to retrieve the &#x60;next&#x60; set of results.
     * @param limit Maximum number of attachments per result to return. If more results exist, use the &#x60;Link&#x60; header to retrieve a relative URL that will return the next set of results.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getPageContentPropertiesOpts.config] Override http request option.
     */
    public getPageContentProperties(page_id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getPageContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<MultiEntityResultContentProperty>>;
    public getPageContentProperties(page_id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getPageContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (page_id === null || page_id === undefined) {
            throw new Error('Required parameter page_id was null or undefined when calling getPageContentProperties.');
        }

        let queryParameters = new URLSearchParams();
        if (key !== undefined && key !== null) {
            queryParameters.append('key', <any>key);
        }
        if (sort !== undefined && sort !== null) {
            queryParameters.append('sort', <any>sort);
        }
        if (cursor !== undefined && cursor !== null) {
            queryParameters.append('cursor', <any>cursor);
        }
        if (limit !== undefined && limit !== null) {
            queryParameters.append('limit', <any>limit);
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<MultiEntityResultContentProperty>(`${this.basePath}/pages/${encodeURIComponent(String(page_id))}/properties`,
                    {
                        params: queryParameters,
                        withCredentials: this.configuration.withCredentials,
                        ...getPageContentPropertiesOpts?.config,
                        headers: {...headers, ...getPageContentPropertiesOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content property for page by id
     * Retrieves a specific Content Property by ID that is attached to a specified page.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the page.
     * @param page_id The ID of the page for which content properties should be returned.
     * @param property_id The ID of the content property being requested.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getPageContentPropertiesByIdOpts.config] Override http request option.
     */
    public getPageContentPropertiesById(page_id: number, property_id: number, getPageContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public getPageContentPropertiesById(page_id: number, property_id: number, getPageContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (page_id === null || page_id === undefined) {
            throw new Error('Required parameter page_id was null or undefined when calling getPageContentPropertiesById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling getPageContentPropertiesById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<ContentProperty>(`${this.basePath}/pages/${encodeURIComponent(String(page_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getPageContentPropertiesByIdOpts?.config,
                        headers: {...headers, ...getPageContentPropertiesByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content properties for Smart Link in the content tree
     * Retrieves Content Properties tied to a specified Smart Link in the content tree.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the Smart Link in the content tree.
     * @param id The ID of the Smart Link in the content tree for which content properties should be returned.
     * @param key Filters the response to return a specific content property with matching key (case sensitive).
     * @param sort Used to sort the result by a particular field.
     * @param cursor Used for pagination, this opaque cursor will be returned in the &#x60;next&#x60; URL in the &#x60;Link&#x60; response header. Use the relative URL in the &#x60;Link&#x60; header to retrieve the &#x60;next&#x60; set of results.
     * @param limit Maximum number of Smart Links per result to return. If more results exist, use the &#x60;Link&#x60; header to retrieve a relative URL that will return the next set of results.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getSmartLinkContentPropertiesOpts.config] Override http request option.
     */
    public getSmartLinkContentProperties(id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getSmartLinkContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<MultiEntityResultContentProperty>>;
    public getSmartLinkContentProperties(id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getSmartLinkContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getSmartLinkContentProperties.');
        }

        let queryParameters = new URLSearchParams();
        if (key !== undefined && key !== null) {
            queryParameters.append('key', <any>key);
        }
        if (sort !== undefined && sort !== null) {
            queryParameters.append('sort', <any>sort);
        }
        if (cursor !== undefined && cursor !== null) {
            queryParameters.append('cursor', <any>cursor);
        }
        if (limit !== undefined && limit !== null) {
            queryParameters.append('limit', <any>limit);
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<MultiEntityResultContentProperty>(`${this.basePath}/embeds/${encodeURIComponent(String(id))}/properties`,
                    {
                        params: queryParameters,
                        withCredentials: this.configuration.withCredentials,
                        ...getSmartLinkContentPropertiesOpts?.config,
                        headers: {...headers, ...getSmartLinkContentPropertiesOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content property for Smart Link in the content tree by id
     * Retrieves a specific Content Property by ID that is attached to a specified Smart Link in the content tree.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the Smart Link in the content tree.
     * @param embed_id The ID of the Smart Link in the content tree for which content properties should be returned.
     * @param property_id The ID of the content property being requested.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getSmartLinkContentPropertiesByIdOpts.config] Override http request option.
     */
    public getSmartLinkContentPropertiesById(embed_id: number, property_id: number, getSmartLinkContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public getSmartLinkContentPropertiesById(embed_id: number, property_id: number, getSmartLinkContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (embed_id === null || embed_id === undefined) {
            throw new Error('Required parameter embed_id was null or undefined when calling getSmartLinkContentPropertiesById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling getSmartLinkContentPropertiesById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<ContentProperty>(`${this.basePath}/embeds/${encodeURIComponent(String(embed_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getSmartLinkContentPropertiesByIdOpts?.config,
                        headers: {...headers, ...getSmartLinkContentPropertiesByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content properties for whiteboard
     * Retrieves Content Properties tied to a specified whiteboard.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the whiteboard.
     * @param id The ID of the whiteboard for which content properties should be returned.
     * @param key Filters the response to return a specific content property with matching key (case sensitive).
     * @param sort Used to sort the result by a particular field.
     * @param cursor Used for pagination, this opaque cursor will be returned in the &#x60;next&#x60; URL in the &#x60;Link&#x60; response header. Use the relative URL in the &#x60;Link&#x60; header to retrieve the &#x60;next&#x60; set of results.
     * @param limit Maximum number of attachments per result to return. If more results exist, use the &#x60;Link&#x60; header to retrieve a relative URL that will return the next set of results.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getWhiteboardContentPropertiesOpts.config] Override http request option.
     */
    public getWhiteboardContentProperties(id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getWhiteboardContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<MultiEntityResultContentProperty>>;
    public getWhiteboardContentProperties(id: number, key?: string, sort?: ContentPropertySortOrder, cursor?: string, limit?: number, getWhiteboardContentPropertiesOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (id === null || id === undefined) {
            throw new Error('Required parameter id was null or undefined when calling getWhiteboardContentProperties.');
        }

        let queryParameters = new URLSearchParams();
        if (key !== undefined && key !== null) {
            queryParameters.append('key', <any>key);
        }
        if (sort !== undefined && sort !== null) {
            queryParameters.append('sort', <any>sort);
        }
        if (cursor !== undefined && cursor !== null) {
            queryParameters.append('cursor', <any>cursor);
        }
        if (limit !== undefined && limit !== null) {
            queryParameters.append('limit', <any>limit);
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<MultiEntityResultContentProperty>(`${this.basePath}/whiteboards/${encodeURIComponent(String(id))}/properties`,
                    {
                        params: queryParameters,
                        withCredentials: this.configuration.withCredentials,
                        ...getWhiteboardContentPropertiesOpts?.config,
                        headers: {...headers, ...getWhiteboardContentPropertiesOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Get content property for whiteboard by id
     * Retrieves a specific Content Property by ID that is attached to a specified whiteboard.  **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to view the whiteboard.
     * @param whiteboard_id The ID of the whiteboard for which content properties should be returned.
     * @param property_id The ID of the content property being requested.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [getWhiteboardContentPropertiesByIdOpts.config] Override http request option.
     */
    public getWhiteboardContentPropertiesById(whiteboard_id: number, property_id: number, getWhiteboardContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public getWhiteboardContentPropertiesById(whiteboard_id: number, property_id: number, getWhiteboardContentPropertiesByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (whiteboard_id === null || whiteboard_id === undefined) {
            throw new Error('Required parameter whiteboard_id was null or undefined when calling getWhiteboardContentPropertiesById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling getWhiteboardContentPropertiesById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
        ];
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.get<ContentProperty>(`${this.basePath}/whiteboards/${encodeURIComponent(String(whiteboard_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...getWhiteboardContentPropertiesByIdOpts?.config,
                        headers: {...headers, ...getWhiteboardContentPropertiesByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Update content property for attachment by id
     * Update a content property for attachment by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the attachment.
     * @param attachment_id The ID of the attachment the property belongs to.
     * @param property_id The ID of the property to be updated.
     * @param ContentPropertyUpdateRequest The content property to be updated.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [updateAttachmentPropertyByIdOpts.config] Override http request option.
     */
    public updateAttachmentPropertyById(attachment_id: string, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateAttachmentPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public updateAttachmentPropertyById(attachment_id: string, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateAttachmentPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (attachment_id === null || attachment_id === undefined) {
            throw new Error('Required parameter attachment_id was null or undefined when calling updateAttachmentPropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling updateAttachmentPropertyById.');
        }

        if (ContentPropertyUpdateRequest === null || ContentPropertyUpdateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyUpdateRequest was null or undefined when calling updateAttachmentPropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.put<ContentProperty>(`${this.basePath}/attachments/${encodeURIComponent(String(attachment_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    ContentPropertyUpdateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...updateAttachmentPropertyByIdOpts?.config,
                        headers: {...headers, ...updateAttachmentPropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Update content property for blog post by id
     * Update a content property for blog post by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the blog post.
     * @param blogpost_id The ID of the blog post the property belongs to.
     * @param property_id The ID of the property to be updated.
     * @param ContentPropertyUpdateRequest The content property to be updated.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [updateBlogpostPropertyByIdOpts.config] Override http request option.
     */
    public updateBlogpostPropertyById(blogpost_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateBlogpostPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public updateBlogpostPropertyById(blogpost_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateBlogpostPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (blogpost_id === null || blogpost_id === undefined) {
            throw new Error('Required parameter blogpost_id was null or undefined when calling updateBlogpostPropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling updateBlogpostPropertyById.');
        }

        if (ContentPropertyUpdateRequest === null || ContentPropertyUpdateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyUpdateRequest was null or undefined when calling updateBlogpostPropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.put<ContentProperty>(`${this.basePath}/blogposts/${encodeURIComponent(String(blogpost_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    ContentPropertyUpdateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...updateBlogpostPropertyByIdOpts?.config,
                        headers: {...headers, ...updateBlogpostPropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Update content property for comment by id
     * Update a content property for a comment by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the comment.
     * @param comment_id The ID of the comment the property belongs to.
     * @param property_id The ID of the property to be updated.
     * @param ContentPropertyUpdateRequest The content property to be updated.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [updateCommentPropertyByIdOpts.config] Override http request option.
     */
    public updateCommentPropertyById(comment_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateCommentPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public updateCommentPropertyById(comment_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateCommentPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (comment_id === null || comment_id === undefined) {
            throw new Error('Required parameter comment_id was null or undefined when calling updateCommentPropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling updateCommentPropertyById.');
        }

        if (ContentPropertyUpdateRequest === null || ContentPropertyUpdateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyUpdateRequest was null or undefined when calling updateCommentPropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.put<ContentProperty>(`${this.basePath}/comments/${encodeURIComponent(String(comment_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    ContentPropertyUpdateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...updateCommentPropertyByIdOpts?.config,
                        headers: {...headers, ...updateCommentPropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Update content property for custom content by id
     * Update a content property for a piece of custom content by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the custom content.
     * @param custom_content_id The ID of the custom content the property belongs to.
     * @param property_id The ID of the property to be updated.
     * @param ContentPropertyUpdateRequest The content property to be updated.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [updateCustomContentPropertyByIdOpts.config] Override http request option.
     */
    public updateCustomContentPropertyById(custom_content_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateCustomContentPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public updateCustomContentPropertyById(custom_content_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateCustomContentPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (custom_content_id === null || custom_content_id === undefined) {
            throw new Error('Required parameter custom_content_id was null or undefined when calling updateCustomContentPropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling updateCustomContentPropertyById.');
        }

        if (ContentPropertyUpdateRequest === null || ContentPropertyUpdateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyUpdateRequest was null or undefined when calling updateCustomContentPropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.put<ContentProperty>(`${this.basePath}/custom-content/${encodeURIComponent(String(custom_content_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    ContentPropertyUpdateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...updateCustomContentPropertyByIdOpts?.config,
                        headers: {...headers, ...updateCustomContentPropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Update content property for database by id
     * Update a content property for a database by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the database.
     * @param database_id The ID of the database the property belongs to.
     * @param property_id The ID of the property to be updated.
     * @param ContentPropertyUpdateRequest The content property to be updated.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [updateDatabasePropertyByIdOpts.config] Override http request option.
     */
    public updateDatabasePropertyById(database_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateDatabasePropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public updateDatabasePropertyById(database_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateDatabasePropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (database_id === null || database_id === undefined) {
            throw new Error('Required parameter database_id was null or undefined when calling updateDatabasePropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling updateDatabasePropertyById.');
        }

        if (ContentPropertyUpdateRequest === null || ContentPropertyUpdateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyUpdateRequest was null or undefined when calling updateDatabasePropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.put<ContentProperty>(`${this.basePath}/databases/${encodeURIComponent(String(database_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    ContentPropertyUpdateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...updateDatabasePropertyByIdOpts?.config,
                        headers: {...headers, ...updateDatabasePropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Update content property for folder by id
     * Update a content property for a folder by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the folder.
     * @param folder_id The ID of the folder the property belongs to.
     * @param property_id The ID of the property to be updated.
     * @param ContentPropertyUpdateRequest The content property to be updated.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [updateFolderPropertyByIdOpts.config] Override http request option.
     */
    public updateFolderPropertyById(folder_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateFolderPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public updateFolderPropertyById(folder_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateFolderPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (folder_id === null || folder_id === undefined) {
            throw new Error('Required parameter folder_id was null or undefined when calling updateFolderPropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling updateFolderPropertyById.');
        }

        if (ContentPropertyUpdateRequest === null || ContentPropertyUpdateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyUpdateRequest was null or undefined when calling updateFolderPropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.put<ContentProperty>(`${this.basePath}/folders/${encodeURIComponent(String(folder_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    ContentPropertyUpdateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...updateFolderPropertyByIdOpts?.config,
                        headers: {...headers, ...updateFolderPropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Update content property for page by id
     * Update a content property for a page by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the page.
     * @param page_id The ID of the page the property belongs to.
     * @param property_id The ID of the property to be updated.
     * @param ContentPropertyUpdateRequest The content property to be updated.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [updatePagePropertyByIdOpts.config] Override http request option.
     */
    public updatePagePropertyById(page_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updatePagePropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public updatePagePropertyById(page_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updatePagePropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (page_id === null || page_id === undefined) {
            throw new Error('Required parameter page_id was null or undefined when calling updatePagePropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling updatePagePropertyById.');
        }

        if (ContentPropertyUpdateRequest === null || ContentPropertyUpdateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyUpdateRequest was null or undefined when calling updatePagePropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.put<ContentProperty>(`${this.basePath}/pages/${encodeURIComponent(String(page_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    ContentPropertyUpdateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...updatePagePropertyByIdOpts?.config,
                        headers: {...headers, ...updatePagePropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Update content property for Smart Link in the content tree by id
     * Update a content property for a Smart Link in the content tree by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the Smart Link in the content tree.
     * @param embed_id The ID of the Smart Link in the content tree the property belongs to.
     * @param property_id The ID of the property to be updated.
     * @param ContentPropertyUpdateRequest The content property to be updated.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [updateSmartLinkPropertyByIdOpts.config] Override http request option.
     */
    public updateSmartLinkPropertyById(embed_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateSmartLinkPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public updateSmartLinkPropertyById(embed_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateSmartLinkPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (embed_id === null || embed_id === undefined) {
            throw new Error('Required parameter embed_id was null or undefined when calling updateSmartLinkPropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling updateSmartLinkPropertyById.');
        }

        if (ContentPropertyUpdateRequest === null || ContentPropertyUpdateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyUpdateRequest was null or undefined when calling updateSmartLinkPropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.put<ContentProperty>(`${this.basePath}/embeds/${encodeURIComponent(String(embed_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    ContentPropertyUpdateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...updateSmartLinkPropertyByIdOpts?.config,
                        headers: {...headers, ...updateSmartLinkPropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
    /**
     * Update content property for whiteboard by id
     * Update a content property for a whiteboard by its id.   **[Permissions](https://confluence.atlassian.com/x/_AozKw) required**: Permission to edit the whiteboard.
     * @param whiteboard_id The ID of the whiteboard the property belongs to.
     * @param property_id The ID of the property to be updated.
     * @param ContentPropertyUpdateRequest The content property to be updated.
     * @param observe set whether or not to return the data Observable as the body, response or events. defaults to returning the body.
     * @param reportProgress flag to report request and response progress.
     * @param {*} [updateWhiteboardPropertyByIdOpts.config] Override http request option.
     */
    public updateWhiteboardPropertyById(whiteboard_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateWhiteboardPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<AxiosResponse<ContentProperty>>;
    public updateWhiteboardPropertyById(whiteboard_id: number, property_id: number, ContentPropertyUpdateRequest: ContentPropertyUpdateRequest, updateWhiteboardPropertyByIdOpts?: { config?: AxiosRequestConfig }): Observable<any> {
        if (whiteboard_id === null || whiteboard_id === undefined) {
            throw new Error('Required parameter whiteboard_id was null or undefined when calling updateWhiteboardPropertyById.');
        }

        if (property_id === null || property_id === undefined) {
            throw new Error('Required parameter property_id was null or undefined when calling updateWhiteboardPropertyById.');
        }

        if (ContentPropertyUpdateRequest === null || ContentPropertyUpdateRequest === undefined) {
            throw new Error('Required parameter ContentPropertyUpdateRequest was null or undefined when calling updateWhiteboardPropertyById.');
        }

        let headers = {...this.defaultHeaders};

        let accessTokenObservable: Observable<any> = of(null);

        // authentication (basicAuth) required
        if (this.configuration.username || this.configuration.password) {
            headers['Authorization'] = 'Basic ' + btoa(this.configuration.username + ':' + this.configuration.password);
        }

        // authentication (oAuthDefinitions) required
        if (this.configuration.accessToken) {
            accessTokenObservable = typeof this.configuration.accessToken === 'function'
                ? from(Promise.resolve(this.configuration.accessToken()))
                : from(Promise.resolve(this.configuration.accessToken))
        }

        // to determine the Accept header
        let httpHeaderAccepts: string[] = [
            'application/json'
        ];
        const httpHeaderAcceptSelected: string | undefined = this.configuration.selectHeaderAccept(httpHeaderAccepts);
        if (httpHeaderAcceptSelected != undefined) {
            headers['Accept'] = httpHeaderAcceptSelected;
        }

        // to determine the Content-Type header
        const consumes: string[] = [
            'application/json'
        ];
        const httpContentTypeSelected: string | undefined = this.configuration.selectHeaderContentType(consumes);
        if (httpContentTypeSelected != undefined) {
            headers['Content-Type'] = httpContentTypeSelected;
        }
        return accessTokenObservable.pipe(
            switchMap((accessToken) => {
                if (accessToken) {
                    headers['Authorization'] = `Bearer ${accessToken}`;
                }

                return this.httpClient.put<ContentProperty>(`${this.basePath}/whiteboards/${encodeURIComponent(String(whiteboard_id))}/properties/${encodeURIComponent(String(property_id))}`,
                    ContentPropertyUpdateRequest,
                    {
                        withCredentials: this.configuration.withCredentials,
                        ...updateWhiteboardPropertyByIdOpts?.config,
                        headers: {...headers, ...updateWhiteboardPropertyByIdOpts?.config?.headers},
                    }
                );
            })
        );
    }
}
